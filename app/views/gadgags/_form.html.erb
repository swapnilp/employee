<%= form_for(@gadgag) do |f| %>
  <% if @gadgag.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@gadgag.errors.count, "error") %> prohibited
        this post from being saved:</h2>
    <ul>
    <% @gadgag.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
